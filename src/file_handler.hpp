#pragma once

// Responsible for dealing with all input reading and output writing
// Both can take a:
//   .txt text file and turn it into a .z78 compressed file
//   .z78 already compressed file and turn it back to a .txt text file

class FileHandler {
public:
  void ReadData();

private:
};
